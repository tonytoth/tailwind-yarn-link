import { Canvas } from '@storybook/addon-docs';
import { Icon, IconType } from 'react-tailwindcss';

<Meta title="DOCS/Icons" />

# Icons

<Canvas>
  <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
    {Object.keys(IconType).map((icon) => (
      <div key={icon} className="text-gray-600" style={{ display: 'flex', alignItems: 'center' }}>
        <Icon className="text-gray-800 group-hover:text-gray-500 mr-3 h-6 w-6" icon={IconType[icon]} />
        &nbsp;
        <span>{icon}</span>
      </div>
    ))}
  </div>
</Canvas>
